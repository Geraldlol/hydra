{
  "name": "vscode-hydra-room",
  "displayName": "Hydra Agents",
  "description": "A multi-head AI collaboration room with evidence-backed decisions, build/review handoffs, and native CLI workflows inside VS Code.",
  "version": "0.8.1",
  "packageManager": "pnpm@11.1.3",
  "publisher": "geraldlol",
  "license": "MIT",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/Geraldlol/hydra.git"
  },
  "homepage": "https://github.com/Geraldlol/hydra#readme",
  "bugs": {
    "url": "https://github.com/Geraldlol/hydra/issues"
  },
  "keywords": [
    "ai",
    "agents",
    "codex",
    "claude",
    "collaboration",
    "automation",
    "review",
    "pair-programming",
    "vscode"
  ],
  "galleryBanner": {
    "color": "#0A0F14",
    "theme": "dark"
  },
  "icon": "media/hydra-heads/guard.png",
  "preview": true,
  "pricing": "Free",
  "qna": false,
  "engines": {
    "vscode": "^1.120.0",
    "node": "^22.13.0 || ^24.0.0"
  },
  "categories": [
    "AI",
    "Chat"
  ],
  "main": "./dist/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "hydraRoom.start",
        "title": "Hydra: Start"
      },
      {
        "command": "hydraRoom.open",
        "title": "Hydra: Open Room"
      },
      {
        "command": "hydraRoom.openBrowser",
        "title": "Hydra: Open Integrated Browser"
      },
      {
        "command": "hydraRoom.toggleBrowserControl",
        "title": "Hydra: Toggle Agent Browser Control"
      },
      {
        "command": "hydraRoom.askBoth",
        "title": "Hydra: Send Message"
      },
      {
        "command": "hydraRoom.assignBuilder",
        "title": "Hydra: Assign Builder"
      },
      {
        "command": "hydraRoom.assignParallelBuilders",
        "title": "Hydra: Assign All Seated Builders"
      },
      {
        "command": "hydraRoom.requestReview",
        "title": "Hydra: Request Review"
      },
      {
        "command": "hydraRoom.runVerification",
        "title": "Hydra: Run Verification"
      },
      {
        "command": "hydraRoom.openVerification",
        "title": "Hydra: Open Verification Log"
      },
      {
        "command": "hydraRoom.previewNextPrompt",
        "title": "Hydra: Preview Next Prompt"
      },
      {
        "command": "hydraRoom.openLastPrompt",
        "title": "Hydra: Open Last Prompt"
      },
      {
        "command": "hydraRoom.attachFiles",
        "title": "Hydra: Attach Files"
      },
      {
        "command": "hydraRoom.cleanWorkspaceState",
        "title": "Hydra: Clean Workspace State"
      },
      {
        "command": "hydraRoom.acceptDefaultDecision",
        "title": "Hydra: Accept Default Decision"
      },
      {
        "command": "hydraRoom.toggleAutoAdvanceActionableDefaults",
        "title": "Hydra: Toggle Agent-default Auto-advance"
      },
      {
        "command": "hydraRoom.commandCenter",
        "title": "Hydra: Command Center"
      },
      {
        "command": "hydraRoom.stop",
        "title": "Hydra: Stop Current Turn"
      },
      {
        "command": "hydraRoom.openTranscript",
        "title": "Hydra: Open Transcript"
      },
      {
        "command": "hydraRoom.archiveAndClearRoom",
        "title": "Hydra: Archive and Clear Room"
      },
      {
        "command": "hydraRoom.openDecisions",
        "title": "Hydra: Open Decisions"
      },
      {
        "command": "hydraRoom.openStandings",
        "title": "Hydra: Open Standings"
      },
      {
        "command": "hydraRoom.openScoreEvidence",
        "title": "Hydra: Review Score Evidence"
      },
      {
        "command": "hydraRoom.recordScoreVerdict",
        "title": "Hydra: Record Evidence Verdict"
      },
      {
        "command": "hydraRoom.reverseScoreVerdict",
        "title": "Hydra: Reverse Evidence Verdict"
      },
      {
        "command": "hydraRoom.adjudicatePendingScoreClaim",
        "title": "Hydra: Adjudicate Pending Score Claim"
      },
      {
        "command": "hydraRoom.openDuels",
        "title": "Hydra: Open Formal Duels"
      },
      {
        "command": "hydraRoom.runDuelReadinessSmokeTest",
        "title": "Hydra: Run Duel Readiness Test"
      },
      {
        "command": "hydraRoom.advanceDuel",
        "title": "Hydra: Advance Formal Duel"
      },
      {
        "command": "hydraRoom.cancelDuel",
        "title": "Hydra: Cancel Formal Duel"
      },
      {
        "command": "hydraRoom.openDuelAudit",
        "title": "Hydra: Open Duel Audit"
      },
      {
        "command": "hydraRoom.correctDuelResult",
        "title": "Hydra: Correct Duel Result"
      },
      {
        "command": "hydraRoom.openSessionBrief",
        "title": "Hydra: Open Session Brief"
      },
      {
        "command": "hydraRoom.openWikiContext",
        "title": "Hydra: Open Wiki Context"
      },
      {
        "command": "hydraRoom.runWikiWrapupNow",
        "title": "Hydra: Run Wiki Wrapup Now"
      },
      {
        "command": "hydraRoom.openSupportBundle",
        "title": "Hydra: Open Support Bundle"
      },
      {
        "command": "hydraRoom.captureNativeCapabilities",
        "title": "Hydra: Capture Native Capabilities"
      },
      {
        "command": "hydraRoom.captureNativeDataSnapshot",
        "title": "Hydra: Capture Native Data Snapshot"
      },
      {
        "command": "hydraRoom.nativeAction",
        "title": "Hydra: Native Action"
      },
      {
        "command": "hydraRoom.openNativeActions",
        "title": "Hydra: Open Native Action Log"
      },
      {
        "command": "hydraRoom.openAgentCalls",
        "title": "Hydra: Open Agent Call Log"
      },
      {
        "command": "hydraRoom.manageFlightRecorder",
        "title": "Hydra: Inspect Flight Recorder"
      },
      {
        "command": "hydraRoom.replayFlightTrace",
        "title": "Hydra: Replay Flight Trace",
        "enablement": "isWorkspaceTrusted"
      },
      {
        "command": "hydraRoom.createFlightEval",
        "title": "Hydra: Create Eval from Flight Trace"
      },
      {
        "command": "hydraRoom.openObjective",
        "title": "Hydra: Open Room Objective"
      },
      {
        "command": "hydraRoom.openMissionContract",
        "title": "Hydra: Open Mission Contract"
      },
      {
        "command": "hydraRoom.manageMissionContract",
        "title": "Hydra: Manage Mission Contract"
      },
      {
        "command": "hydraRoom.proposeMissionContract",
        "title": "Hydra: Propose or Amend Mission Contract"
      },
      {
        "command": "hydraRoom.admitMissionProposal",
        "title": "Hydra: Admit Agent Mission Proposal"
      },
      {
        "command": "hydraRoom.confirmMissionContract",
        "title": "Hydra: Confirm Mission Contract"
      },
      {
        "command": "hydraRoom.dismissMissionContractProposal",
        "title": "Hydra: Dismiss Mission Contract Proposal"
      },
      {
        "command": "hydraRoom.retireMissionContract",
        "title": "Hydra: Retire Mission Contract"
      },
      {
        "command": "hydraRoom.openNativeTerminals",
        "title": "Hydra: Advanced: Open Native Terminals"
      },
      {
        "command": "hydraRoom.pokeCodexTerminal",
        "title": "Hydra: Poke Codex Terminal"
      },
      {
        "command": "hydraRoom.runCodexCommand",
        "title": "Hydra: Run Codex Native Command"
      },
      {
        "command": "hydraRoom.sendCodexRawTerminalLine",
        "title": "Hydra: Send Codex Raw Terminal Line"
      },
      {
        "command": "hydraRoom.pokeCodexTerminalWithEditor",
        "title": "Hydra: Poke Codex With Editor Context"
      },
      {
        "command": "hydraRoom.pokeCodexTerminalWithDiff",
        "title": "Hydra: Poke Codex With Working Tree"
      },
      {
        "command": "hydraRoom.pokeClaudeTerminal",
        "title": "Hydra: Poke Claude Terminal"
      },
      {
        "command": "hydraRoom.runClaudeCommand",
        "title": "Hydra: Run Claude Native Command"
      },
      {
        "command": "hydraRoom.sendClaudeRawTerminalLine",
        "title": "Hydra: Send Claude Raw Terminal Line"
      },
      {
        "command": "hydraRoom.pokeClaudeTerminalWithEditor",
        "title": "Hydra: Poke Claude With Editor Context"
      },
      {
        "command": "hydraRoom.pokeClaudeTerminalWithDiff",
        "title": "Hydra: Poke Claude With Working Tree"
      },
      {
        "command": "hydraRoom.pokeBothTerminals",
        "title": "Hydra: Poke Both Native Terminals"
      },
      {
        "command": "hydraRoom.pokeBothTerminalsWithEditor",
        "title": "Hydra: Poke Both With Editor Context"
      },
      {
        "command": "hydraRoom.pokeBothTerminalsWithDiff",
        "title": "Hydra: Poke Both With Working Tree"
      },
      {
        "command": "hydraRoom.useTerminalBridge",
        "title": "Hydra: Advanced: Use Experimental Terminal Bridge"
      },
      {
        "command": "hydraRoom.runTerminalBridgeSelfTest",
        "title": "Hydra: Advanced: Run Terminal Bridge Self-Test"
      },
      {
        "command": "hydraRoom.runMissionFlightSmokeTest",
        "title": "Hydra: Advanced: Run Mission + Flight Recorder Smoke Test"
      },
      {
        "command": "hydraRoom.runArenaSmokeTest",
        "title": "Hydra: Advanced: Run Arena Worktree Smoke Test",
        "enablement": "isWorkspaceTrusted"
      },
      {
        "command": "hydraRoom.manageArenaResults",
        "title": "Hydra: Manage Arena Results"
      },
      {
        "command": "hydraRoom.recoverArenaRuns",
        "title": "Hydra: Review Arena Recovery"
      },
      {
        "command": "hydraRoom.runManyHeadsSmokeTest",
        "title": "Hydra: Advanced: Run Claude Worker Fanout Smoke Test",
        "enablement": "isWorkspaceTrusted"
      },
      {
        "command": "hydraRoom.showEffectiveAuthority",
        "title": "Hydra: Show Effective Native Authority"
      },
      {
        "command": "hydraRoom.showTerminalBridgeHealth",
        "title": "Hydra: Advanced: Show Terminal Bridge Health"
      },
      {
        "command": "hydraRoom.runDoctor",
        "title": "Hydra: Run Doctor"
      },
      {
        "command": "hydraRoom.autopilotStart",
        "title": "Hydra: Autopilot Start"
      },
      {
        "command": "hydraRoom.fixCodexPath",
        "title": "Hydra: Setup: Fix Codex CLI Path"
      },
      {
        "command": "hydraRoom.fixClaudePath",
        "title": "Hydra: Setup: Fix Claude CLI Path"
      },
      {
        "command": "hydraRoom.resetStuckTurn",
        "title": "Hydra: Advanced: Reset Stuck Turn"
      },
      {
        "command": "hydraRoom.useOneShotTransport",
        "title": "Hydra: Advanced: Use Safe One-Shot Transport"
      },
      {
        "command": "hydraRoom.insertPromptTemplate",
        "title": "Hydra: Insert Prompt Template"
      },
      {
        "command": "hydraRoom.chooseModel",
        "title": "Hydra: Choose Model"
      },
      {
        "command": "hydraRoom.changeCapabilityProfile",
        "title": "Hydra: Change Capability Profile"
      },
      {
        "command": "hydraRoom.refreshCodexModels",
        "title": "Hydra: Refresh Codex Models"
      },
      {
        "command": "hydraRoom.chooseEffort",
        "title": "Hydra: Choose Thinking Level"
      },
      {
        "command": "hydraRoom.testTelegram",
        "title": "Hydra: Send Test Telegram Message"
      }
    ],
    "keybindings": [
      {
        "command": "hydraRoom.stop",
        "key": "ctrl+alt+x",
        "mac": "cmd+alt+x"
      },
      {
        "command": "hydraRoom.acceptDefaultDecision",
        "key": "ctrl+alt+a",
        "mac": "cmd+alt+a"
      },
      {
        "command": "hydraRoom.assignBuilder",
        "key": "ctrl+alt+b",
        "mac": "cmd+alt+b"
      },
      {
        "command": "hydraRoom.requestReview",
        "key": "ctrl+alt+r",
        "mac": "cmd+alt+r"
      },
      {
        "command": "hydraRoom.commandCenter",
        "key": "ctrl+alt+h",
        "mac": "cmd+alt+h"
      },
      {
        "command": "hydraRoom.insertPromptTemplate",
        "key": "ctrl+alt+t",
        "mac": "cmd+alt+t"
      },
      {
        "command": "hydraRoom.chooseModel",
        "key": "ctrl+alt+m",
        "mac": "cmd+alt+m"
      },
      {
        "command": "hydraRoom.chooseEffort",
        "key": "ctrl+alt+e",
        "mac": "cmd+alt+e"
      }
    ],
    "configuration": {
      "title": "Hydra Room",
      "properties": {
        "hydraRoom.transcriptPath": {
          "scope": "application",
          "type": "string",
          "default": ".hydra/transcript.md",
          "markdownDescription": "Workspace-relative or absolute path where Hydra writes the Markdown transcript."
        },
        "hydraRoom.workspaceRoot": {
          "scope": "application",
          "type": "string",
          "default": "",
          "markdownDescription": "Advanced fallback project root for Extension Development Host sessions without an open VS Code workspace. Normal users should leave this blank and open a folder in VS Code."
        },
        "hydraRoom.codexCommand": {
          "scope": "application",
          "type": "string",
          "default": "codex",
          "markdownDescription": "Executable name or wrapper script for the OpenAI Codex CLI."
        },
        "hydraRoom.agents": {
          "scope": "application",
          "type": "array",
          "default": [],
          "items": {
            "type": "object",
            "required": [
              "id",
              "displayName",
              "kind"
            ],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
              },
              "displayName": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "codex",
                  "claude",
                  "gemini",
                  "openai-compatible",
                  "cli-template"
                ]
              },
              "model": {
                "type": "string"
              },
              "colorIndex": {
                "type": "integer",
                "minimum": 1,
                "maximum": 8
              },
              "baseUrl": {
                "type": "string",
                "markdownDescription": "openai-compatible: `https://` required unless the host is loopback or a private/`.local` address (local model servers)."
              },
              "apiKeyEnv": {
                "type": "string",
                "markdownDescription": "NAME of the environment variable holding the API key. Never inline the key itself."
              },
              "headers": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                },
                "markdownDescription": "Extra HTTP headers. Reference secrets as `${env:NAME}`."
              },
              "maxOutputTokens": {
                "type": "integer",
                "minimum": 1,
                "maximum": 131072,
                "default": 4096,
                "markdownDescription": "openai-compatible: maximum completion tokens requested per response. Defaults to 4096 so a transport timeout cannot become an unbounded token/spend allowance."
              },
              "command": {
                "type": "string"
              },
              "argsTemplate": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "markdownDescription": "cli-template argv. Placeholders: `${prompt}`, `${model}`, `${workspaceFolder}`, `${hydraPromptFile}`, `${hydraReplyFile}`, `${hydraLogFile}`."
              },
              "defaultAuthority": {
                "type": "string",
                "enum": [
                  "read-only",
                  "workspace-write",
                  "full-native"
                ]
              },
              "pricing": {
                "type": "object",
                "properties": {
                  "inputPerMTok": {
                    "type": "number",
                    "minimum": 0,
                    "description": "USD per million input tokens."
                  },
                  "outputPerMTok": {
                    "type": "number",
                    "minimum": 0,
                    "description": "USD per million output tokens."
                  },
                  "cacheReadPerMTok": {
                    "type": "number",
                    "minimum": 0,
                    "description": "USD per million cache-read tokens."
                  },
                  "cacheCreatePerMTok": {
                    "type": "number",
                    "minimum": 0,
                    "description": "USD per million cache-write tokens."
          }
        },
                "additionalProperties": false,
                "markdownDescription": "Override per-model USD pricing (per million tokens) used for usage-rail cost estimates."
              }
            },
            "additionalProperties": false
          },
          "markdownDescription": "Custom Hydra heads (open/local models). Each entry is an agent definition; user entries override built-ins by `id`. **Security:** trust-scoped — ignored in untrusted workspaces. API keys must be referenced by env-var name (`apiKeyEnv`), never inlined; definitions containing key-shaped values are rejected."
        },
        "hydraRoom.roomRoster": {
          "scope": "application",
          "type": "array",
          "default": [
            "codex",
            "claude"
          ],
          "minItems": 2,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$"
          },
          "markdownDescription": "Ordered agent IDs active in this Hydra room. IDs must name built-in heads or entries in `hydraRoom.agents`. The first ID is the default opener and later IDs are eligible reactors/reviewers. Stored as a User setting because changing the roster changes which native agents Hydra can dispatch."
        },
        "hydraRoom.codexExecArgsDiscussion": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "exec",
            "--sandbox",
            "workspace-write",
            "--color",
            "never",
            "--cd",
            "${workspaceFolder}",
            "--skip-git-repo-check",
            "-"
          ],
          "markdownDescription": "Raw Codex CLI args for serialized discussion turns. `${workspaceFolder}` is expanded; the trailing `-` reads the Hydra prompt from stdin. Hydra also expands `${hydraPromptFile}`, `${hydraReplyFile}`, and `${hydraLogFile}` per request so native file/output flags can target Hydra artifacts."
        },
        "hydraRoom.codexExecArgsBuild": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "exec",
            "--sandbox",
            "workspace-write",
            "--color",
            "never",
            "--cd",
            "${workspaceFolder}",
            "--skip-git-repo-check",
            "-"
          ],
          "markdownDescription": "Raw Codex CLI args for Build. Defaults to workspace-write `codex exec`; add native flags such as config profiles, model, search, MCP/plugin-related config, sandbox changes, or Hydra request-file placeholders here."
        },
        "hydraRoom.codexExecArgsReview": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "review",
            "--uncommitted",
            "-"
          ],
          "markdownDescription": "Raw Codex CLI args for Review. Defaults to Codex's native review command over staged, unstaged, and untracked changes. Hydra does not downgrade these args by phase and expands Hydra request-file placeholders."
        },
        "hydraRoom.codexDiscussionProfile": {
          "scope": "application",
          "type": "string",
          "enum": [
            "safeDiscussion",
            "nativeDiscussion",
            "nativeBuild",
            "nativeReview",
            "fullNative",
            "custom"
          ],
          "default": "safeDiscussion",
          "markdownDescription": "Capability profile for Codex discussion turns. The default `safeDiscussion` profile is read-only. `fullNative` remains an explicit opt-in and requires per-workspace consent. `custom` preserves `hydraRoom.codexExecArgsDiscussion`."
        },
        "hydraRoom.codexBuildProfile": {
          "scope": "application",
          "type": "string",
          "enum": [
            "safeDiscussion",
            "nativeDiscussion",
            "nativeBuild",
            "nativeReview",
            "fullNative",
            "custom"
          ],
          "default": "nativeBuild",
          "markdownDescription": "Capability profile for Codex Build turns. The default `nativeBuild` profile permits workspace edits while the Codex sandbox keeps `.git` read-only and command network access off. `fullNative` remains an explicit opt-in and requires per-workspace consent. `custom` preserves `hydraRoom.codexExecArgsBuild`."
        },
        "hydraRoom.codexReviewProfile": {
          "scope": "application",
          "type": "string",
          "enum": [
            "safeDiscussion",
            "nativeDiscussion",
            "nativeBuild",
            "nativeReview",
            "fullNative",
            "custom"
          ],
          "default": "nativeReview",
          "markdownDescription": "Capability profile for Codex Review turns. The default `nativeReview` profile is read-only. `fullNative` remains an explicit opt-in and requires per-workspace consent. `custom` preserves `hydraRoom.codexExecArgsReview`."
        },
        "hydraRoom.codexCaptureLastMessage": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When dispatching `codex exec`, automatically add Codex's native `--output-last-message` file capture and use that final answer in the room. This keeps stderr/plugin noise out of Hydra replies while preserving full diagnostics in `.hydra/agent-calls.jsonl`. Superseded by `hydraRoom.codexJson` when enabled (the JSON stream already carries the final agent message)."
        },
        "hydraRoom.codexJson": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When dispatching `codex exec` through safe one-shot transport, automatically request the typed event stream via `--json`, parse it through Hydra's `codexEvents` module, and use the final `agent_message` item as the reply in the room. Structured tool-call / command-execution / file-change events are summarized in `.hydra/agent-calls.jsonl`. Supersedes `hydraRoom.codexCaptureLastMessage` when both are enabled. Defaults to `true`: the parsed-event stream lets Hydra stream Codex's reply live (matching Claude's `claudeStreamJson`) and gives token-accurate Codex usage accounting. Set to `false` to fall back to plain one-shot text. The experimental terminal bridge always uses structured Codex output so the visible terminal does not echo the full prompt transcript."
        },
        "hydraRoom.claudeCommand": {
          "scope": "application",
          "type": "string",
          "default": "claude",
          "markdownDescription": "Executable name or wrapper script for the Claude Code CLI."
        },
        "hydraRoom.claudeExecArgsDiscussion": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "-p",
            "--permission-mode",
            "acceptEdits",
            "--add-dir",
            "${workspaceFolder}"
          ],
          "markdownDescription": "Raw Claude Code CLI args for serialized discussion turns. `${workspaceFolder}` is expanded. Hydra also expands `${hydraPromptFile}`, `${hydraReplyFile}`, and `${hydraLogFile}` per request so native file/output flags can target Hydra artifacts."
        },
        "hydraRoom.claudeExecArgsBuild": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "-p",
            "--permission-mode",
            "acceptEdits",
            "--add-dir",
            "${workspaceFolder}"
          ],
          "markdownDescription": "Raw Claude Code CLI args for Build. Defaults to print mode with edit-accepting permissions; add native flags such as --ide, --tools, --mcp-config, --plugin-dir, --agent, --settings, model, effort, or Hydra request-file placeholders here."
        },
        "hydraRoom.claudeExecArgsReview": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "-p",
            "--permission-mode",
            "acceptEdits",
            "--add-dir",
            "${workspaceFolder}"
          ],
          "markdownDescription": "Raw Claude Code CLI args for Review. Add native review/tool/MCP/plugin/model flags here. Hydra does not downgrade these args by phase and expands Hydra request-file placeholders."
        },
        "hydraRoom.claudeDiscussionProfile": {
          "scope": "application",
          "type": "string",
          "enum": [
            "safeDiscussion",
            "nativeDiscussion",
            "nativeBuild",
            "nativeReview",
            "fullNative",
            "custom"
          ],
          "default": "safeDiscussion",
          "markdownDescription": "Capability profile for Claude discussion turns. The default `safeDiscussion` profile uses Claude `--safe-mode`, plan permission mode, and an explicit read/search-only tool list; non-managed hooks, plugins, skills, commands, agents, MCP, browser, and session persistence stay disabled. Organization-managed Claude policy may still apply. `fullNative` remains an explicit opt-in and requires per-workspace consent. `custom` preserves `hydraRoom.claudeExecArgsDiscussion`."
        },
        "hydraRoom.claudeBuildProfile": {
          "scope": "application",
          "type": "string",
          "enum": [
            "safeDiscussion",
            "nativeDiscussion",
            "nativeBuild",
            "nativeReview",
            "fullNative",
            "custom"
          ],
          "default": "nativeBuild",
          "markdownDescription": "Capability profile for Claude Build turns. The default `nativeBuild` profile uses Claude `--safe-mode` and exposes only read/edit/write notebook tools; non-managed shell, web, MCP, browser, skill, subagent, hook, project/local-setting, and session-persistence surfaces stay disabled. Organization-managed Claude policy may still apply. `fullNative` remains an explicit opt-in and requires per-workspace consent. `custom` preserves `hydraRoom.claudeExecArgsBuild`."
        },
        "hydraRoom.claudeReviewProfile": {
          "scope": "application",
          "type": "string",
          "enum": [
            "safeDiscussion",
            "nativeDiscussion",
            "nativeBuild",
            "nativeReview",
            "fullNative",
            "custom"
          ],
          "default": "nativeReview",
          "markdownDescription": "Capability profile for Claude Review turns. The default `nativeReview` profile uses Claude `--safe-mode`, plan permission mode, and an explicit read/search-only tool list; non-managed hooks, plugins, skills, commands, agents, MCP, browser, and session persistence stay disabled. Organization-managed Claude policy may still apply. `fullNative` remains an explicit opt-in and requires per-workspace consent. `custom` preserves `hydraRoom.claudeExecArgsReview`."
        },
        "hydraRoom.claudeStreamJson": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When dispatching Claude print mode without an explicit output format, automatically request `--output-format stream-json --verbose`, parse the final assistant text for the room, and keep structured tool-event output in `.hydra/agent-calls.jsonl`."
        },
        "hydraRoom.claudeStreamJsonIncludePartialMessages": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When `hydraRoom.claudeStreamJson` is enabled, also request Claude's partial message stream so Hydra can summarize tool-use and task lifecycle events in `.hydra/agent-calls.jsonl`."
        },
        "hydraRoom.claudeDebugFile": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When `hydraRoom.claudeStreamJson` is enabled, pass Claude `--debug-file ${hydraLogFile}` so raw native debug output is captured in the per-request Hydra log artifact."
        },
        "hydraRoom.geminiCommand": {
          "scope": "application",
          "type": "string",
          "default": "gemini",
          "markdownDescription": "Executable name or wrapper script for the Google Gemini CLI."
        },
        "hydraRoom.geminiExecArgsDiscussion": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "--output-format",
            "json"
          ],
          "markdownDescription": "Raw Gemini CLI args for serialized discussion turns. Hydra pipes the prompt on stdin, which activates Gemini headless mode; `--output-format json` supplies the documented response and per-model SessionMetrics envelope used for safe reply and usage parsing. Hydra also expands `${workspaceFolder}`, `${hydraPromptFile}`, `${hydraReplyFile}`, and `${hydraLogFile}` per request."
        },
        "hydraRoom.geminiExecArgsBuild": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "--output-format",
            "json"
          ],
          "markdownDescription": "Raw Gemini CLI args for Build. Hydra pipes the prompt on stdin; the default documented JSON envelope enables authoritative reply extraction and token accounting. Add native model, sandbox/approval, or Hydra request-file flags here as needed."
        },
        "hydraRoom.geminiExecArgsReview": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "--output-format",
            "json"
          ],
          "markdownDescription": "Raw Gemini CLI args for Review. Hydra pipes the prompt on stdin and parses the default documented JSON envelope. Hydra does not downgrade these args by phase and expands Hydra request-file placeholders."
        },
        "hydraRoom.manyHeadsMode": {
          "scope": "application",
          "type": "boolean",
          "default": false,
          "markdownDescription": "Claude Worker Fanout (experimental; legacy key name): use up to `hydraRoom.manyHeadsClaudeWorkerCount` local subscription-backed Claude processes in one-shot Discussion, Build, and Review. Build extras are isolated no-tool advisers that drain before one lead may edit; Review duplicates collapse to one Claude roster verdict. Workers receive trace/live files but are not independent Hydra head identities. Stored as a User setting and forced off in untrusted workspaces."
        },
        "hydraRoom.agentInitiatedDuels": {
          "scope": "application",
          "type": "boolean",
          "default": true,
          "markdownDescription": "Allow a reactor or closer head to initiate a rated formal duel from its own reply. Hydra policy validates and admits the challenge, then runs both sealed full-native commitments automatically. The human remains the independent adjudicator. Stored as a User setting and forced off in untrusted workspaces."
        },
        "hydraRoom.manyHeadsClaudeWorkerCount": {
          "scope": "application",
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 8,
          "markdownDescription": "Maximum local subscription-backed Claude worker count for one-shot Discussion, Build, and Review fanout (1-8). For Build this count includes the sole lead; remaining workers are isolated read-only advisers. This multiplies official Claude Code / Agent SDK dispatches and therefore Agent SDK credit usage; workspace overrides are ignored in untrusted workspaces."
        },
        "hydraRoom.nativePathPrepend": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Directories prepended to PATH/Path for every spawned native head, including Codex, Claude, and Gemini, in both one-shot and terminal-bridge transports. Supports `${workspaceFolder}` and `${env:NAME}` placeholders."
        },
        "hydraRoom.nativeEnv": {
          "scope": "application",
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "string"
          },
          "markdownDescription": "Environment variables applied to every spawned native head, including Codex, Claude, and Gemini, in both one-shot and terminal-bridge transports. Values support `${workspaceFolder}` and `${env:NAME}` placeholders."
        },
        "hydraRoom.codexNativePathPrepend": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Codex-only directories appended after `hydraRoom.nativePathPrepend` when constructing PATH/Path for native Codex dispatches. Supports `${workspaceFolder}` and `${env:NAME}` placeholders."
        },
        "hydraRoom.codexNativeEnv": {
          "scope": "application",
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "string"
          },
          "markdownDescription": "Codex-only environment variables layered over `hydraRoom.nativeEnv` for native Codex dispatches. Values support `${workspaceFolder}` and `${env:NAME}` placeholders."
        },
        "hydraRoom.claudeNativePathPrepend": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Claude-only directories appended after `hydraRoom.nativePathPrepend` when constructing PATH/Path for native Claude dispatches. Supports `${workspaceFolder}` and `${env:NAME}` placeholders."
        },
        "hydraRoom.claudeNativeEnv": {
          "scope": "application",
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "string"
          },
          "markdownDescription": "Claude-only environment variables layered over `hydraRoom.nativeEnv` for native Claude dispatches. Values support `${workspaceFolder}` and `${env:NAME}` placeholders."
        },
        "hydraRoom.geminiNativePathPrepend": {
          "scope": "application",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Gemini-only directories appended after `hydraRoom.nativePathPrepend` when constructing PATH/Path for native Gemini dispatches. Supports `${workspaceFolder}` and `${env:NAME}` placeholders."
        },
        "hydraRoom.geminiNativeEnv": {
          "scope": "application",
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "string"
          },
          "markdownDescription": "Gemini-only environment variables layered over `hydraRoom.nativeEnv` for native Gemini dispatches. Values support `${workspaceFolder}` and `${env:NAME}` placeholders."
        },
        "hydraRoom.transport": {
          "type": "string",
          "enum": [
            "oneShot",
            "terminalBridge"
          ],
          "default": "oneShot",
          "markdownDescription": "Deprecated. Hydra transport is session-only and always starts in safe one-shot mode. Use the in-room advanced button or commands to temporarily enable the experimental terminal bridge."
        },
        "hydraRoom.autopilotOnStart": {
          "scope": "application",
          "type": "boolean",
          "default": true,
          "markdownDescription": "Run Hydra Autopilot when the room opens: Doctor checks, terminal bridge self-test, and automatic transport selection."
        },
        "hydraRoom.preferTerminalBridgeOnStart": {
          "scope": "application",
          "type": "boolean",
          "default": false,
          "markdownDescription": "When Autopilot passes the terminal bridge self-test, automatically use the visible native terminal bridge instead of safe one-shot transport. Stored as a User setting and forced off in untrusted workspaces because the terminal bridge is experimental."
        },
        "hydraRoom.codexTerminalCommand": {
          "scope": "application",
          "type": "string",
          "default": "",
          "markdownDescription": "Optional startup command sent once when Hydra opens the Codex terminal. Leave blank for command-dispatch mode. `${workspaceFolder}` is expanded."
        },
        "hydraRoom.claudeTerminalCommand": {
          "scope": "application",
          "type": "string",
          "default": "",
          "markdownDescription": "Optional startup command sent once when Hydra opens the Claude terminal. Leave blank for command-dispatch mode. `${workspaceFolder}` is expanded."
        },
        "hydraRoom.geminiTerminalCommand": {
          "scope": "application",
          "type": "string",
          "default": "",
          "markdownDescription": "Optional startup command sent once when Hydra opens the Gemini terminal. Leave blank for command-dispatch mode. `${workspaceFolder}` is expanded."
        },
        "hydraRoom.terminalStartupDelayMs": {
          "type": "number",
          "default": 1000,
          "minimum": 0,
          "markdownDescription": "Maximum milliseconds to wait for the visible native terminal startup probe after running an agent terminal startup command."
        },
        "hydraRoom.terminalReplyPollMs": {
          "type": "number",
          "default": 500,
          "minimum": 100,
          "markdownDescription": "Polling interval for reply files in VS Code's private per-workspace extension storage when using `terminalBridge` transport."
        },
        "hydraRoom.terminalBridgeTimeoutMs": {
          "type": "number",
          "default": 45000,
          "minimum": 5000,
          "markdownDescription": "Maximum time to wait for terminal bridge self-tests and setup checks. Normal agent calls use the Discussion or Build/Review timeout settings."
        },
        "hydraRoom.oneShotWorkspaceInstructionsMaxChars": {
          "default": {
            "discussion": 12000,
            "build": 12000,
            "review": 12000
          },
          "anyOf": [
            {
              "type": "number",
              "minimum": 0,
              "description": "Character cap applied to every phase. Set to 0 to include the loaded instruction context without an additional prompt-time cap."
            },
            {
              "type": "object",
              "properties": {
                "discussion": {
                  "type": "number",
                  "minimum": 0
                },
                "build": {
                  "type": "number",
                  "minimum": 0
                },
                "review": {
                  "type": "number",
                  "minimum": 0
                }
              },
              "additionalProperties": false,
              "description": "Per-phase repository-instruction caps. Discussion covers opener, reactor, closer, and parallel discussion turns."
            }
          ],
          "markdownDescription": "Maximum characters of repository instruction files injected into one-shot Hydra prompts after filtering out the recipient CLI's own native instruction files. Set a single number for every phase, or use `{ \"discussion\": 12000, \"build\": 12000, \"review\": 12000 }`. A value of 0 includes the loaded instruction context without an additional prompt-time cap for that phase."
        },
        "hydraRoom.terminalBridgeWorkspaceInstructionsMaxChars": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "markdownDescription": "Maximum characters of repository instruction files injected into visible native-terminal prompts after filtering out the recipient CLI's own native instruction files. Set to 0 to skip Hydra-injected repository instructions in terminal bridge prompts."
        },
        "hydraRoom.promptTranscriptMaxChars": {
          "default": {
            "discussion": 80000,
            "build": 400000,
            "review": 400000
          },
          "anyOf": [
            {
              "type": "number",
              "minimum": 0,
              "description": "Character cap applied to every phase. Set to 0 to keep the full active transcript and disable threshold refreshes."
            },
            {
              "type": "object",
              "properties": {
                "discussion": {
                  "type": "number",
                  "minimum": 0
                },
                "build": {
                  "type": "number",
                  "minimum": 0
                },
                "review": {
                  "type": "number",
                  "minimum": 0
                }
              },
              "additionalProperties": false,
              "description": "Per-phase transcript caps. Discussion covers opener, reactor, closer, and parallel discussion turns."
            }
          ],
          "markdownDescription": "Maximum characters of active room transcript injected into agent prompts after objective, workspace instructions, and wiki context. This is a per-call cost and attention guardrail, not a model context-window limit. Hydra keeps the newest transcript messages and adds an omission notice when older active history is trimmed. Either set one number for every phase, or use `{ \"discussion\": 80000, \"build\": 400000, \"review\": 400000 }`. The wiki context refresh threshold uses the largest configured cap, so the default refreshes at the 400k durable-memory boundary while discussion prompts stay lean."
        },
        "hydraRoom.editorContextMaxChars": {
          "type": "number",
          "default": 12000,
          "minimum": 1000,
          "markdownDescription": "Maximum characters from the active editor selection or file attached to direct native terminal pokes."
        },
        "hydraRoom.attachmentPreviewMaxChars": {
          "type": "number",
          "default": 12000,
          "minimum": 0,
          "markdownDescription": "Maximum text-preview characters injected into a room turn for each uploaded file. Hydra still copies the full file into `.hydra/attachments/` for the agents to inspect directly. Set to 0 to attach paths without inline previews."
        },
        "hydraRoom.attachmentMaxBytes": {
          "scope": "application",
          "type": "number",
          "default": 10485760,
          "minimum": 0,
          "markdownDescription": "Maximum bytes allowed for a single file attached to a room turn. Defaults to 10 MiB."
        },
        "hydraRoom.attachmentTotalMaxBytes": {
          "scope": "application",
          "type": "number",
          "default": 33554432,
          "minimum": 0,
          "markdownDescription": "Maximum total bytes allowed across pending room attachments. Defaults to 32 MiB."
        },
        "hydraRoom.wikiContextMaxChars": {
          "type": "number",
          "default": 8000,
          "minimum": 0,
          "markdownDescription": "Maximum characters of compiled `.hydra/wiki` context injected into agent prompts. Set to 0 to disable wiki prompt context."
        },
        "hydraRoom.wikiPromptIncludeLog": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Include append-only `.hydra/wiki/log.md` entries in every agent prompt. Disabled by default so routine prompts spend wiki budget on compiled `context.md` and `index.md`; wrapups still read the log tail for maintenance."
        },
        "hydraRoom.wikiWrapupEnabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "After a successful discussion turn, ask one native agent to distill durable facts into `.hydra/wiki/context.md`, update `.hydra/wiki/index.md` when useful, append `.hydra/wiki/log.md`, and save a raw source snapshot under `.hydra/wiki/raw/turns/` when the wiki changes. Disable to keep the wiki manual-only."
        },
        "hydraRoom.wikiWrapupAgent": {
          "type": "string",
          "enum": [
            "auto",
            "codex",
            "claude"
          ],
          "default": "auto",
          "markdownDescription": "Agent used for automatic wiki wrapups. `auto` chooses the lower estimated configured discussion-model cost from `hydraRoom.modelPrices`; `codex` or `claude` pins the maintainer."
        },
        "hydraRoom.wikiWrapupMaxSourceChars": {
          "type": "number",
          "default": 16000,
          "minimum": 1000,
          "markdownDescription": "Maximum characters from the just-finished room turn sent to the automatic wiki wrapup prompt."
        },
        "hydraRoom.wikiWrapupTimeoutMs": {
          "type": "number",
          "default": 120000,
          "minimum": 1000,
          "markdownDescription": "Maximum runtime in milliseconds for the background automatic wiki wrapup agent call."
        },
        "hydraRoom.wikiRawTurnsKeepDays": {
          "type": "number",
          "default": 30,
          "minimum": 0,
          "markdownDescription": "Days of immutable `.hydra/wiki/raw/turns/` snapshots to keep after automatic wiki wrapups. Set to 0 to keep raw turn snapshots indefinitely."
        },
        "hydraRoom.promptBodyRetentionDays": {
          "scope": "application",
          "type": "number",
          "default": 3,
          "minimum": 0,
          "markdownDescription": "Days to keep full rendered prompt bodies in `.hydra/prompts/index.jsonl` before `Hydra: Clean Workspace State` compacts them to metadata-only records. Set to 0 to compact prompt bodies whenever cleanup runs."
        },
        "hydraRoom.diagnosticRetentionDays": {
          "scope": "application",
          "type": "number",
          "default": 7,
          "minimum": 0,
          "markdownDescription": "Days to keep copied attachments and legacy workspace-local terminal diagnostics before `Hydra: Clean Workspace State` deletes them. Current terminal-bridge artifacts live in VS Code extension storage and use a short automatic retention sweep. `.hydra/prompts/index.jsonl` is preserved and compacted separately."
        },
        "hydraRoom.firstSpeaker": {
          "scope": "application",
          "type": "string",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$",
          "default": "codex",
          "markdownDescription": "Default opener for serialized discussion turns. Set this to any registered, seated head ID; if that head is not seated, Hydra falls back to the first roster entry. The webview can change the opener per turn without changing this setting."
        },
        "hydraRoom.discussionMode": {
          "type": "string",
          "enum": [
            "parallelOnBoth",
            "serial",
            "parallel"
          ],
          "default": "parallelOnBoth",
          "markdownDescription": "How normal room messages run Discussion. `parallelOnBoth` is retained for compatibility: Hydra uses the serialized opener -> reactor -> optional closer loop unless the user explicitly addresses the group, in which case every seated head runs independently. `serial` always uses the serialized loop. `parallel` always dispatches the entire seated roster and returns after all replies finish, trading less critique choreography for lower wall-clock latency."
        },
        "hydraRoom.oneShotTimeoutMs": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "markdownDescription": "Maximum runtime per Build or Review agent call in milliseconds. Set to `0` to disable the wall-clock cap and rely on Stop Current Turn."
        },
        "hydraRoom.discussionTimeoutMs": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "markdownDescription": "Maximum runtime per Discussion opener, reactor, closer, or parallel call in milliseconds. Set to `0` to disable the wall-clock cap and rely on Stop Current Turn."
        },
        "hydraRoom.diffMaxLines": {
          "type": "number",
          "default": 6000,
          "minimum": 100,
          "markdownDescription": "Soft cap for `git diff` output before truncation in the Review prompt."
        },
        "hydraRoom.verifyCommand": {
          "scope": "application",
          "type": "string",
          "default": "",
          "markdownDescription": "Shell command Hydra runs for one-click verification. Leave blank to infer from package.json scripts in this order: `verify:fast`, `verify`, `check && test`, `test`, `check`, then `lint`. Inference is gated by Workspace Trust; untrusted workspaces refuse inferred package.json commands."
        },
        "hydraRoom.verifyTimeoutMs": {
          "type": "number",
          "default": 600000,
          "minimum": 1000,
          "markdownDescription": "Maximum runtime for Hydra verification commands in milliseconds."
        },
        "hydraRoom.verifyMaxOutputChars": {
          "type": "number",
          "default": 12000,
          "minimum": 1000,
          "markdownDescription": "Maximum stdout/stderr characters retained from each verification run."
        },
        "hydraRoom.autoVerifyAfterBuild": {
          "scope": "application",
          "type": "boolean",
          "default": true,
          "markdownDescription": "Automatically run Hydra verification after a successful Build phase."
        },
        "hydraRoom.autoScorePassingBuilds": {
          "scope": "application",
          "type": "boolean",
          "default": true,
          "markdownDescription": "Automatically add deterministic passive-score evidence when a serial builder changes the Git-visible workspace and Hydra verifies that exact post-build state under an unchanged eligible package-script plan and bounded verifier-control inventory captured before dispatch. Eligible package-manager executables are resolved outside the workspace; repeated receipts under one plan/control regime share a correlated round. Unsupported/dynamic commands may still verify but do not score. No-op, parallel, failed, cancelled, timed-out, unconfirmed, control-changing, or state-changing runs are never scored. Standings never change agent authority."
        },
        "hydraRoom.autoAdvanceActionableDefaults": {
          "scope": "application",
          "type": "boolean",
          "default": false,
          "markdownDescription": "Controls agent-default auto-advance. Decision Packet defaults are agent-authored and may be influenced by prompt injection from repository content. Off by default. Enabling requires an explicit modal opt-in in a trusted workspace and can automatically execute eligible follow-up actions without a separate Accept Default click. Manual Accept Default remains available. Stored as a User setting and forced off in untrusted workspaces."
        },
        "hydraRoom.autoSkipCloserOnAgreement": {
          "scope": "application",
          "type": "boolean",
          "default": true,
          "markdownDescription": "Skip the discussion closer when the reactor begins with `Agree:` and its Decision Packet has `Decision needed from user: none` and `Blockers: none`. Disable to force the traditional opener/reactor/closer cadence."
        },
        "hydraRoom.reviewConvergence": {
          "scope": "application",
          "type": "string",
          "enum": [
            "human",
            "unanimous",
            "majority"
          ],
          "default": "human",
          "markdownDescription": "How Hydra folds multiple independent review verdicts. `human` advances unanimous approval but requires an explicit Accept despite dissent or Hand back choice whenever a multi-reviewer panel has any dissent. `unanimous` requires every reviewer to approve. `majority` requires strictly more than half; a tie never approves. Single-reviewer behavior is unchanged."
        },
        "hydraRoom.reviewParticipation": {
          "scope": "application",
          "type": "string",
          "enum": [
            "single",
            "all"
          ],
          "default": "all",
          "markdownDescription": "Who reviews a single builder's diff. `all` sends every other seated head an independent review in parallel; `single` sends only the first eligible non-builder. The builder is excluded in both modes."
        },
        "hydraRoom.autoAdvanceSendInstructionMaxConsecutive": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "markdownDescription": "Maximum number of consecutive auto-advanced send-instruction turns before Hydra pauses and waits for Accept Default. Prevents runaway discussion loops when agents keep emitting non-blocking free-form defaults. Phase-changing auto-advances (assign builder, request review, hand back) reset the counter."
        },
        "hydraRoom.sessionCostCapUsd": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "markdownDescription": "If greater than 0, Hydra pauses auto-advance once the session's estimated cost reaches this USD value. Cost is computed from `.hydra/usage.jsonl` using `hydraRoom.modelPrices`. Set to 0 to disable. Useful guard rail when running autonomous loops. Codex JSON event accounting is enabled by default. If you disable `hydraRoom.codexJson`, the one-shot plain-text fallback has no per-token usage breakdown, so Hydra estimates conservatively by billing the whole reply at the output rate — the displayed figure (and therefore when this cap trips) runs high."
        },
        "hydraRoom.claudeAutomationCreditGuard": {
          "type": "string",
          "enum": [
            "off",
            "warn",
            "blockManyHeads",
            "blockClaudeAutomation"
          ],
          "enumDescriptions": [
            "Never warn or block on Claude automation spend.",
            "Surface a warning once monthly Claude automation spend reaches the cap.",
            "Block Claude Worker Fanout over the cap; single Claude turns still warn.",
            "Block all Claude automation turns once monthly spend reaches the cap."
          ],
          "default": "warn",
          "markdownDescription": "How Hydra reacts when **subscription-backed** Claude automation spend reaches `hydraRoom.claudeAgentCreditCapUsd` in the current calendar month. As of 2026-06-15, subscription `claude -p` / Agent SDK usage draws from a capped monthly Agent SDK credit pool instead of pay-as-you-go API billing, so heavy Hydra automation can exhaust it. Only applies when Claude is authenticated via a subscription (claude.ai / a plan); API-key auth is pay-as-you-go and is never warned or blocked. Spend is summed from `.hydra/usage.jsonl` (Claude rows only). This setting only affects Hydra's own cost/decision logic — it injects into no CLI spawn — so it is window/resource scoped, not `application`."
        },
        "hydraRoom.claudeAgentCreditCapUsd": {
          "type": "number",
          "default": 200,
          "minimum": 0,
          "markdownDescription": "Monthly Claude automation spend ceiling in USD that `hydraRoom.claudeAutomationCreditGuard` checks against. Defaults to `200` (the Max 20x / eligible Enterprise Premium Agent SDK monthly credit). Set to `0` to disable the threshold. Spend is summed per UTC calendar month from Claude rows in `.hydra/usage.jsonl`; prefer native `total_cost_usd` when Claude stream-json is enabled."
        },
        "hydraRoom.claudeAgentEstimatedRunCostUsd": {
          "type": "number",
          "default": 1,
          "minimum": 0,
          "markdownDescription": "Estimated USD reservation Hydra adds before dispatching each Claude automation call when evaluating `hydraRoom.claudeAutomationCreditGuard`. Defaults to `$1` so Claude Worker Fanout cannot launch many workers against the same stale recorded spend. Set to `0` to disable in-flight projection. This only affects Hydra's own cost/decision logic and is not trust-scoped."
        },
        "hydraRoom.handoffWebhookUrl": {
          "scope": "application",
          "type": "string",
          "default": "",
          "markdownDescription": "Optional HTTPS endpoint Hydra POSTs to when a new decision packet has a non-empty `Decision needed from user` field. Hydra rejects URL credentials, redirects, and any destination whose complete DNS answer set contains a private or otherwise non-public address; the request is pinned to a vetted address. Body is `{ event: \"hydra.decision_needed\", timestamp, workspace, agent, phase, decisionNeededFromUser, defaultNextAction, recommendation, blockers, objective }`. Use a Slack incoming webhook URL or your own paging service. For Telegram, use `hydraRoom.telegramBotToken` + `hydraRoom.telegramChatId` instead — Hydra speaks the Telegram Bot API directly. Dedupes per packet timestamp."
        },
        "hydraRoom.telegramBotToken": {
          "scope": "application",
          "type": "string",
          "default": "",
          "markdownDescription": "Telegram Bot API token (the long `123456:ABC-DEF…` string from BotFather). Empty disables Telegram. **Treat as secret** — prefer User Settings over Workspace Settings so it doesn't get committed. Alternatively set the `TELEGRAM_BOT_TOKEN` environment variable and leave this blank."
        },
        "hydraRoom.telegramChatId": {
          "scope": "application",
          "type": "string",
          "default": "",
          "markdownDescription": "Telegram chat id Hydra sends notifications to. Your personal chat with the bot, a group, or a channel. Find it by messaging your bot and visiting `https://api.telegram.org/bot<token>/getUpdates`. Required for Telegram notifications to fire. **Security:** when inbound polling is on, authorization is chat-level — pointing this at a **group** lets *every member of that group* (and anyone who obtains its invite link) drive agent turns in your workspace. Prefer your private 1:1 chat with the bot, or set `hydraRoom.telegramInboundAllowedSenderIds` to gate individual senders."
        },
        "hydraRoom.telegramNotifyOnDecisionNeeded": {
          "scope": "application",
          "type": "boolean",
          "default": true,
          "markdownDescription": "Send a Telegram message when a decision packet's `Decision needed from user` is non-empty. Requires `hydraRoom.telegramBotToken` and `hydraRoom.telegramChatId`."
        },
        "hydraRoom.telegramInboundPollingEnabled": {
          "scope": "application",
          "type": "boolean",
          "default": false,
          "markdownDescription": "Poll Telegram Bot API updates and inject accepted messages into the Hydra room as user input. Requires `hydraRoom.telegramBotToken`, `hydraRoom.telegramChatId`, and the `hydraRoom.telegramInboundCommandPrefix` prefix. Disabled by default because inbound Telegram messages are remote untrusted input. **Authorization is chat-level, not per-sender:** anyone who can post to the configured `hydraRoom.telegramChatId` can drive agent turns under the configured execution permissions — keep that chat private (a 1:1 with the bot, or a group of fully-trusted members)."
        },
        "hydraRoom.telegramLiveSteeringEnabled": {
          "scope": "application",
          "type": "boolean",
          "default": false,
          "markdownDescription": "Allow explicitly routed Telegram messages beginning with `hydraRoom.telegramLiveSteeringCommandPrefix` to steer an already-active native run without interrupting it. Requires inbound polling, an exact configured chat, and a **non-empty** `hydraRoom.telegramInboundAllowedSenderIds` list. Disabled by default because steering can consume paid provider work."
        },
        "hydraRoom.telegramLiveSteeringCommandPrefix": {
          "scope": "application",
          "type": "string",
          "default": "/steer",
          "pattern": "^/[A-Za-z0-9_]{1,31}$",
          "markdownDescription": "Separate slash-command prefix for Telegram live steering. Reply to a Hydra-routed Telegram message with `/steer <instruction>`, or use `/hydra <room-token> /steer <instruction>`. Telegram text never selects the native target or changes its Mission/authority bindings."
        },
        "hydraRoom.telegramInboundCommandPrefix": {
          "scope": "application",
          "type": "string",
          "default": "/hydra",
          "markdownDescription": "Required prefix for Telegram inbound polling. Messages from the configured chat must start with this prefix (for example `/hydra continue with the default`) before Hydra treats them as room input. Set to an empty string only for a private bot chat you fully trust."
        },
        "hydraRoom.telegramInboundAllowedSenderIds": {
          "scope": "application",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "markdownDescription": "Optional per-sender allowlist for Telegram inbound polling. Empty (the default) means every sender in the configured `hydraRoom.telegramChatId` may drive agent turns — for a group chat, that is every member. Set it to a list of Telegram numeric user ids (as strings, e.g. `[\"123456789\"]`) to accept inbound commands ONLY from those senders; every other sender in the chat is silently ignored. Find an id by messaging `@userinfobot`, or by reading `from.id` from `https://api.telegram.org/bot<token>/getUpdates`. Requires `hydraRoom.telegramInboundPollingEnabled`."
        },
        "hydraRoom.telegramInboundPollIntervalSeconds": {
          "scope": "application",
          "type": "number",
          "default": 10,
          "minimum": 5,
          "markdownDescription": "How often Hydra polls Telegram for inbound commands when `hydraRoom.telegramInboundPollingEnabled` is enabled."
        },
        "hydraRoom.promptTemplates": {
          "type": "array",
          "default": [],
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string"
              },
              "body": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "body"
            ]
          },
          "markdownDescription": "Saved composer templates. Run `Hydra: Insert Prompt Template` (or its keybinding `Ctrl+Alt+T`) to pick one; the body replaces the current composer text. Example: `[{ \"name\": \"Survey before code\", \"body\": \"Survey the affected files first. List what you'd change before making any edits.\" }]`."
        },
        "hydraRoom.claudeModel": {
          "scope": "application",
          "default": "",
          "anyOf": [
            {
              "type": "string",
              "description": "Model applied to every Claude phase. Empty = CLI default."
            },
            {
              "type": "object",
              "properties": {
                "discussion": {
                  "type": "string"
                },
                "build": {
                  "type": "string"
                },
                "review": {
                  "type": "string"
                }
              },
              "additionalProperties": false,
              "description": "Per-phase models for Claude. Missing phases fall back to the CLI default."
            }
          ],
          "markdownDescription": "Model passed to Claude Code CLI as `--model <id>` on every dispatch. Either a single string (applied to every phase) or an object with per-phase overrides like `{ \"discussion\": \"sonnet\", \"build\": \"opus\", \"review\": \"fable\" }`. Aliases (`fable`, `sonnet`, `opus`, `haiku`) always resolve to the current build of that family; or pin a full ID (`claude-fable-5-1`, `claude-sonnet-5`, `claude-opus-5`, `claude-haiku-4-5-20251001`). Use `Hydra: Choose Model` (Ctrl+Alt+M) to change live."
        },
        "hydraRoom.codexModel": {
          "scope": "application",
          "default": "",
          "anyOf": [
            {
              "type": "string",
              "description": "Model applied to every Codex phase. Empty = CLI default."
            },
            {
              "type": "object",
              "properties": {
                "discussion": {
                  "type": "string"
                },
                "build": {
                  "type": "string"
                },
                "review": {
                  "type": "string"
                }
              },
              "additionalProperties": false,
              "description": "Per-phase models for Codex. Missing phases fall back to the CLI default."
            }
          ],
          "markdownDescription": "Model passed to Codex CLI as `--model <id>` on every `exec` dispatch. Either a single string (applied to every phase) or an object with per-phase overrides. Current Codex model IDs include `gpt-5.6-sol` (`gpt-5.6` alias), `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-daybreak-blue-latest` (Daybreak-program alias of the current flagship), `gpt-5.5`, `gpt-5.4`, `gpt-5.4-mini`, and `gpt-5.3-codex-spark` (interactive only, no API). Run `codex debug models` (or `Hydra: Refresh Codex Models`) to inspect what your install actually exposes. Use `Hydra: Choose Model` (Ctrl+Alt+M) to change live."
        },
        "hydraRoom.geminiModel": {
          "scope": "application",
          "default": "",
          "anyOf": [
            {
              "type": "string",
              "description": "Model applied to every Gemini phase. Empty = CLI default."
            },
            {
              "type": "object",
              "properties": {
                "discussion": {
                  "type": "string"
                },
                "build": {
                  "type": "string"
                },
                "review": {
                  "type": "string"
                }
              },
              "additionalProperties": false,
              "description": "Per-phase models for Gemini. Missing phases fall back to the CLI default."
            }
          ],
          "markdownDescription": "Model passed to the Gemini CLI as `--model <id>`. Either a single string (applied to every phase) or a per-phase object. Prefer the current CLI aliases `auto`, `pro`, `flash`, and `flash-lite`; concrete choices include `gemini-3.1-pro-preview`, `gemini-3.5-flash`, `gemini-3.1-flash-lite`, and the stable Gemini 2.5 family. The CLI default is used when empty."
        },
        "hydraRoom.claudeEffort": {
          "scope": "application",
          "default": "",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "",
                "low",
                "medium",
                "high",
                "xhigh",
                "max"
              ]
            },
            {
              "type": "object",
              "properties": {
                "discussion": {
                  "type": "string",
                  "enum": [
                    "",
                    "low",
                    "medium",
                    "high",
                    "xhigh",
                    "max"
                  ]
                },
                "build": {
                  "type": "string",
                  "enum": [
                    "",
                    "low",
                    "medium",
                    "high",
                    "xhigh",
                    "max"
                  ]
                },
                "review": {
                  "type": "string",
                  "enum": [
                    "",
                    "low",
                    "medium",
                    "high",
                    "xhigh",
                    "max"
                  ]
                }
              },
              "additionalProperties": false
            }
          ],
          "markdownDescription": "Reasoning/effort level passed to Claude as `--effort <level>` on every dispatch. Either a single string (applied to every phase) or an object with per-phase overrides like `{ \"discussion\": \"low\", \"build\": \"high\" }`. Levels: `low`, `medium`, `high`, `xhigh`, `max`. Leave blank to use the CLI's default. If you've already added `--effort` to `hydraRoom.claudeExecArgs*`, the explicit arg wins. Use `Hydra: Choose Thinking Level` (Ctrl+Alt+E)."
        },
        "hydraRoom.codexReasoning": {
          "scope": "application",
          "default": "",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "",
                "none",
                "minimal",
                "low",
                "medium",
                "high",
                "xhigh",
                "max",
                "ultra"
              ]
            },
            {
              "type": "object",
              "properties": {
                "discussion": {
                  "type": "string",
                  "enum": [
                    "",
                    "none",
                    "minimal",
                    "low",
                    "medium",
                    "high",
                    "xhigh",
                    "max",
                    "ultra"
                  ]
                },
                "build": {
                  "type": "string",
                  "enum": [
                    "",
                    "none",
                    "minimal",
                    "low",
                    "medium",
                    "high",
                    "xhigh",
                    "max",
                    "ultra"
                  ]
                },
                "review": {
                  "type": "string",
                  "enum": [
                    "",
                    "none",
                    "minimal",
                    "low",
                    "medium",
                    "high",
                    "xhigh",
                    "max",
                    "ultra"
                  ]
                }
              },
              "additionalProperties": false
            }
          ],
          "markdownDescription": "Reasoning effort passed to Codex as `-c model_reasoning_effort=<level>` on every `exec` dispatch. Either a single string or per-phase object. Levels can vary by model/runtime; common values include `none`, `minimal`, `low`, `medium`, `high`, `xhigh`, `max`, and `ultra`. Run `Hydra: Refresh Codex Models` then check the chooser description for the model's `default_reasoning_level`. Leave blank to use the CLI's default. If `-c model_reasoning_effort=` is already in `hydraRoom.codexExecArgs*`, the explicit arg wins. Use `Hydra: Choose Thinking Level` (Ctrl+Alt+E)."
        },
        "hydraRoom.modelPrices": {
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "object",
            "properties": {
              "inputPerMTok": {
                "type": "number",
                "minimum": 0,
                "description": "USD per million input (prompt) tokens."
              },
              "outputPerMTok": {
                "type": "number",
                "minimum": 0,
                "description": "USD per million output (completion) tokens."
              },
              "cacheReadPerMTok": {
                "type": "number",
                "minimum": 0,
                "description": "USD per million cache-read tokens."
              },
              "cacheCreatePerMTok": {
                "type": "number",
                "minimum": 0,
                "description": "USD per million cache-creation tokens."
              }
            },
            "additionalProperties": false
          },
          "markdownDescription": "Per-agent token pricing for the session cost meter, USD per **million** tokens. The keys `claude` and `codex` set each agent's default price; any other key is treated as a per-model override matched by model id (lower-cased), for example `\"gpt-5.5\"` or `\"opus\"`. Each value object accepts `inputPerMTok`, `outputPerMTok`, `cacheReadPerMTok`, and `cacheCreatePerMTok`, all non-negative numbers. Omitted fields fall back to Hydra's built-in defaults (Sonnet 4.6 pricing for Claude, a GPT-5-family blend for Codex). Example: `{ \"claude\": { \"inputPerMTok\": 3, \"outputPerMTok\": 15, \"cacheReadPerMTok\": 0.3, \"cacheCreatePerMTok\": 3.75 }, \"codex\": { \"inputPerMTok\": 1.25, \"outputPerMTok\": 10 } }`."
        },
        "hydraRoom.autoRequestReviewAfterPassingVerification": {
          "scope": "application",
          "type": "boolean",
          "default": false,
          "markdownDescription": "Automatically request Review after an automatic post-build verification passes. Off by default so users can inspect build output first."
        }
      }
    }
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "Hydra can spawn Codex, Claude, Gemini, and configured native heads and can run a configured verify command. In untrusted workspaces, Hydra ignores workspace-scoped settings for CLI paths, args, env, terminal startup, verify command, automatic execution controls, handoff webhook, and Telegram credentials so a hostile repo cannot redirect or auto-start those sinks.",
      "restrictedConfigurations": [
        "hydraRoom.transcriptPath",
        "hydraRoom.workspaceRoot",
        "hydraRoom.codexCommand",
        "hydraRoom.agents",
        "hydraRoom.roomRoster",
        "hydraRoom.reviewConvergence",
        "hydraRoom.reviewParticipation",
        "hydraRoom.codexExecArgsDiscussion",
        "hydraRoom.codexExecArgsBuild",
        "hydraRoom.codexExecArgsReview",
        "hydraRoom.codexDiscussionProfile",
        "hydraRoom.codexBuildProfile",
        "hydraRoom.codexReviewProfile",
        "hydraRoom.claudeCommand",
        "hydraRoom.claudeExecArgsDiscussion",
        "hydraRoom.claudeExecArgsBuild",
        "hydraRoom.claudeExecArgsReview",
        "hydraRoom.claudeModel",
        "hydraRoom.codexModel",
        "hydraRoom.claudeEffort",
        "hydraRoom.codexReasoning",
        "hydraRoom.claudeDiscussionProfile",
        "hydraRoom.claudeBuildProfile",
        "hydraRoom.claudeReviewProfile",
        "hydraRoom.manyHeadsMode",
        "hydraRoom.agentInitiatedDuels",
        "hydraRoom.manyHeadsClaudeWorkerCount",
        "hydraRoom.nativePathPrepend",
        "hydraRoom.nativeEnv",
        "hydraRoom.codexNativePathPrepend",
        "hydraRoom.codexNativeEnv",
        "hydraRoom.claudeNativePathPrepend",
        "hydraRoom.claudeNativeEnv",
        "hydraRoom.geminiCommand",
        "hydraRoom.geminiExecArgsDiscussion",
        "hydraRoom.geminiExecArgsBuild",
        "hydraRoom.geminiExecArgsReview",
        "hydraRoom.geminiModel",
        "hydraRoom.geminiNativeEnv",
        "hydraRoom.geminiNativePathPrepend",
        "hydraRoom.codexTerminalCommand",
        "hydraRoom.claudeTerminalCommand",
        "hydraRoom.geminiTerminalCommand",
        "hydraRoom.autopilotOnStart",
        "hydraRoom.preferTerminalBridgeOnStart",
        "hydraRoom.verifyCommand",
        "hydraRoom.autoVerifyAfterBuild",
        "hydraRoom.autoScorePassingBuilds",
        "hydraRoom.autoAdvanceActionableDefaults",
        "hydraRoom.autoSkipCloserOnAgreement",
        "hydraRoom.autoRequestReviewAfterPassingVerification",
        "hydraRoom.promptBodyRetentionDays",
        "hydraRoom.diagnosticRetentionDays",
        "hydraRoom.attachmentMaxBytes",
        "hydraRoom.attachmentTotalMaxBytes",
        "hydraRoom.handoffWebhookUrl",
        "hydraRoom.telegramBotToken",
        "hydraRoom.telegramChatId",
        "hydraRoom.telegramNotifyOnDecisionNeeded",
        "hydraRoom.telegramInboundPollingEnabled",
        "hydraRoom.telegramLiveSteeringEnabled",
        "hydraRoom.telegramLiveSteeringCommandPrefix",
        "hydraRoom.telegramInboundCommandPrefix",
        "hydraRoom.telegramInboundPollIntervalSeconds",
        "hydraRoom.telegramInboundAllowedSenderIds"
      ]
    }
  },
  "scripts": {
    "compile": "tsc --incremental -p ./",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "dev": "node scripts/open-dev-host.js",
    "watch": "tsc -watch -p ./",
    "preview:webview": "node scripts/preview-webview.mjs",
    "check": "tsc --noEmit --incremental false -p ./",
    "format:check": "node scripts/check-source-format.js",
    "lint": "corepack pnpm run check && corepack pnpm run format:check",
    "probe:native-contract": "node scripts/native-contract-probe.js",
    "probe:native-contract:execute": "node scripts/native-contract-probe.js --execute",
    "copy-fixtures": "node -e \"const fs=require('node:fs');if(fs.existsSync('test/fixtures'))fs.cpSync('test/fixtures','dist/test/fixtures',{recursive:true})\"",
    "setup-vscode-stub": "node scripts/setup-vscode-stub.js",
    "test": "corepack pnpm run clean && corepack pnpm run test:fast",
    "test:fast": "tsc --incremental -p ./ && corepack pnpm run copy-fixtures && corepack pnpm run setup-vscode-stub && node --test --test-concurrency=4 dist/test/*.test.js",
    "test:coverage": "corepack pnpm run clean && tsc --incremental -p ./ && corepack pnpm run copy-fixtures && corepack pnpm run setup-vscode-stub && node --require=./scripts/coverage-child-isolation.js --test --test-concurrency=4 --experimental-test-coverage --test-coverage-include=dist/src/**/*.js --test-coverage-exclude=dist/src/arenaFakeHeadCli.js --test-coverage-exclude=dist/src/windowsProcessTreeHost.js --test-coverage-exclude=dist/src/panel.js --test-coverage-exclude=dist/src/extension.js --test-coverage-lines=80 --test-coverage-branches=70 --test-coverage-functions=80 dist/test/*.test.js",
    "test:integration": "corepack pnpm run compile && vscode-test --label extensionHost",
    "verify:fast": "corepack pnpm run format:check && corepack pnpm run test:fast",
    "prepackage": "corepack pnpm run test",
    "package": "node scripts/verify-vsix-reproducibility.js",
    "prepackage:pre-release": "corepack pnpm run test",
    "package:pre-release": "node scripts/verify-vsix-reproducibility.js --pre-release",
    "verify:vsix": "node scripts/verify-vsix-contents.js",
    "digest:vsix": "node scripts/write-vsix-digest.js",
    "sbom:vsix": "node scripts/write-vsix-sbom.js",
    "install:local": "node scripts/install-local.js",
    "install:local:existing": "node scripts/install-local.js --skip-package",
    "install:handoff-skill": "node scripts/install-handoff-skill.js"
  },
  "devDependencies": {
    "@types/node": "^22.18.10",
    "@types/vscode": "1.120.0",
    "@vscode/test-cli": "0.0.15",
    "@vscode/test-electron": "3.1.0",
    "@vscode/vsce": "^3.9.2",
    "typescript": "^5.9.3"
  }
}
